import { Logo } from '@/components/Logo';

export default function DamagePolicyPage() {
  return (
    <main className="mx-auto max-w-2xl px-6 py-10">
      <Logo />
      <h1 className="mt-10 font-display text-3xl">Damage policy</h1>
      <p className="mt-4 text-sm text-ink-300">
        Pre and post photos are required on every wash. Residents file claims in the app. Operators respond with their photos. Lavo support decides within five business days. Payouts may come from insurance or the dispute reserve.
      </p>
    </main>
  );
}
